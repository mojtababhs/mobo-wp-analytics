<?php
if (!defined('WP_UNINSTALL_PLUGIN')) exit;

/*
 * حذف کامل داده‌ها به‌صورت پیش‌فرض غیرفعال است.
 *
 * global $wpdb;
 * $wpdb->query("DROP TABLE IF EXISTS {$wpdb->prefix}mobo_analytics_events");
 * delete_option('mobo_analytics_db_version');
 * delete_option('mobo_analytics_settings');
 */
